{
  "name": "heavywatal.github.io",
  "version": "1.0.0",
  "scripts": {
    "stop": "ls -d public/* | grep -Ev '.git|offline|slides' | xargs rm -r",
    "rebuild": "run-s stop build",
    "build": "hugo -D",
    "start": "hugo -D -w"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.3"
  }
}
